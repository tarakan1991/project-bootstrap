# AGENTS.md — {{PROJECT_NAME}}

> Корневой роутер для ИИ-агентов и новых разработчиков: что за проект, глобальные правила, протокол задачи и карта — где что искать.
> Полная информация — в `docs/`, не здесь. Это файл-бюджет: кладём только то, что нельзя вывести из кода или одним поиском.
>
> **Нельзя хранить здесь:** токены, пароли, ключи доступа, строки подключения с учётными данными. Адреса окружений не секрет — они живут в `docs/workflow/environments.md`.
> Файл подключён как `CLAUDE.md` (для Claude Code) через однострочный импорт `@AGENTS.md` — правки делаем только здесь.

---

## Проект

**{{PROJECT_NAME}}** — {{одно предложение: что за продукт и для кого}}.

{{1–2 предложения: текущее состояние, направление развития.}} Полнее — `docs/product.md`.

**Участники:** {{имя — роль; …}}. ИИ-агенты: {{какие}}.
{{Задачи → ссылка на трекер — регламент в `docs/workflow/project-workflow.md`. Строка включается только если задачи где-то ведутся.}}

## Стек

| Компонент | Технология |
|---|---|
| {{компонент}} | {{технология}} |

<!-- Стек не выбран → вместо таблицы одна строка: «Стек не выбран; при выборе — зафиксировать здесь и оформить ADR почему». -->

## Глобальные правила

<!-- Строки набираются по ответам интервью — список в templates/fragments/agents-rules.md.
     Включать ТОЛЬКО соответствующие выбранному: лишняя строка стоит внимания модели. -->

{{строки правил}}

## Протокол задачи

0. {{Открой `docs/README.md` — полная карта документации и таблица «Дома по триггеру»; без неё не начинай. Пункт включается только в режиме карты «большой проект» — в малом карта ниже целиком, и пункта 0 нет.}}
1. {{Прочитай задачу — критерий готовности неясен, уточни ДО начала работы. Пункт включается, если задачи где-то ведутся.}}
2. Сверься с `docs/product.md` (сценарии, правила предметной области) и `docs/decisions/` — план не противоречит принятым решениям.
3. Ветки и коммиты — по `docs/workflow/git-workflow.md`.
4. Работаешь в модуле — прочти его `<модуль>/AGENTS.md`; файла нет → создай по шаблону из `docs/workflow/agents.md` в том же изменении.
5. Инварианты, конвенции и политика тестов — `docs/workflow/conventions.md`.
6. Перед вливанием: {{полная проверка одной командой (`conventions.md`)}} + каскадное обновление доков (`docs/workflow/agents.md`).
7. {{Приём изменений — по разделу в `git-workflow.md`.}}
8. {{Отчёт в задачу и её закрытие — по `project-workflow.md`. Пункт включается, если задачи где-то ведутся.}}

## Карта документации

<!-- Два режима — критерий и механика в SKILL.md (шаг 6), выбранный режим называется человеку вслух:
     • «малый проект» — карта здесь ЦЕЛИКОМ: каждый созданный документ отдельной строкой с пометкой,
       КОГДА его читать («правишь схему данных → data-model.md»). docs/README.md остаётся домом
       таблицы триггеров и справочного, из критического пути задачи выпадает.
     • «большой проект» (≥3 модулей ИЛИ полная карта не умещается в ~12 строк) — здесь только
       горячие ссылки, нужные в большинстве задач; полная карта — docs/README.md, её чтение —
       пункт 0 протокола. Усиливать призыв прилагательными («ВАЖНО: изучи docs/») бесполезно —
       работает императив в нумерованном протоколе, а не баннер.
     Проект вырос — миграция «малый → большой» по строке в «Дома по триггеру». -->

- `docs/product.md` — продукт: пользователи, сценарии, правила предметной области, не-цели
- `docs/architecture/` — как устроено сейчас{{: перечислить созданные файлы}}
- `docs/decisions/` — ADR: почему так, а не иначе (append-only, нумерованные)
- `docs/workflow/` — регламенты: {{перечислить созданные: git-workflow · conventions · agents · project-workflow}}
- Дома по триггеру (design, api, glossary, research, runbooks, environments, …) — таблица в `docs/README.md`

**Операционка модуля** (команды, грабли) → `<модуль>/AGENTS.md`, создаются по триггеру.
**Личное и времянки** → `CLAUDE.local.md`, `scratch/` (в git не попадают).
