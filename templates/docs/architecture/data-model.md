# Данные

> Модель данных и правила работы со схемой. Почему так — в `../decisions/`.

<!-- Файл создаётся ТОЛЬКО если по вопросу 5 есть содержание. «Не выбрано» → файла нет, есть строка в триггерах docs/README.md. -->

## Хранилище

{{из вопроса 5: БД/хранилище и его статус (выбрано / план).}}

## Миграции

Схема меняется **только через миграции** ({{инструмент}}). Правка модели = миграция в том же PR — инвариант из `../workflow/conventions.md`.

## Источник истины схемы для ИИ-агентов

{{из вопроса 5: где агент читает актуальную схему — модели в коде (путь) / миграции / генерируемый дамп схемы — и как её получить (команда).}}

## Концептуальная модель

{{сущности и связи, если уже понятны; иначе секцию удалить — появятся первые таблицы, будет поддерживаться здесь.}}
