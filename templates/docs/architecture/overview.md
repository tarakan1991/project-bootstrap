# Архитектура: обзор системы

> Как система устроена **сейчас**. Почему так — в `../decisions/`. Модуль разросся → отдельный `docs/architecture/<модуль>.md`, здесь остаётся картина целиком.

<!-- Файл создаётся ТОЛЬКО если по вопросу 3 есть содержание. «Не проектировал» → файла нет, есть строка в триггерах docs/README.md. -->

## Форма

{{из вопроса 3: что это физически (веб-сервис / бот / CLI / связка) и из каких крупных частей состоит.}}

## Компоненты и связи

{{список или схема. Если это план, а не реализация — пометить явно: «план, код ещё не написан».}}

## Ограничения

{{известные лимиты и допущения (нагрузка, внешние API, безопасность). Нечего сказать — секцию удалить, не выдумывать.}}
