# Рабочий процесс: задачи

> Регламент работы с задачами. Механика репозитория — `git-workflow.md`.

## Где живут задачи

{{Ссылка на систему.}}
<!-- Внешний трекер с доступом по API: -->{{Доступ по API есть; ключ — в переменной окружения `{{ИМЯ_ПЕРЕМЕННОЙ}}` (объявлена в `env.example`, значение в репозитории не хранится).}}

## Правила

<!-- Строки — по ответам интервью. Лишние удалить. -->

- {{Старт: «Нет задачи — нет работы: любая нетривиальная работа начинается с задачи (исключение — тривиальные правки доков и мелочи)» / «Крупная работа начинается с задачи; мелочь можно без»}}
- {{Связь: «Изменение упоминает задачу в описании» — при необходимости дополнить: «при начале работы задача переводится в „в работу“, при отправке на проверку — в соответствующий статус»}}
- {{Отчёт: «По завершении — отчёт в задачу по шаблону ниже, обязательно» / «По завершении — отчёт в задачу желателен»}}
- {{Закрытие: «Закрывает автор после вливания» / «Закрывает автор после проверки на проде» / «Закрывает постановщик после проверки»}}
- Задачи не ведутся в markdown-файлах репозитория (принцип 5 в `docs/README.md`).

## Шаблон отчёта

По завершении работы — комментарий в задачу:

```
Сделано: <что именно, 1–3 строки>
Как проверить: <шаги или команда>
Не сделано / хвосты: <что осталось и почему; «нет», если чисто>
```

## Критерий готовности

<!-- Строки — по ответам интервью. -->

- Изменения влиты по регламенту `git-workflow.md`{{, проверка пройдена}}
- {{Изменения проверены — при автоматическом выкате на проде; при выкате по кнопке — после того, как выкат запущен}}
- Отчёт в задаче написан, задача закрыта по правилу выше
- Затронута архитектура → в том же изменении обновлён `docs/architecture/*`; нетривиальное решение → ADR

## Детали

Детали внутри самого трекера (шаблоны задач, поля, доски, статусы) ведутся в нём же. Понадобилось зафиксировать договорённость письменно — дописать сюда.
