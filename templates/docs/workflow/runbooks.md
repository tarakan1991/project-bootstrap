# Runbooks — что делается руками

> Сценарии для ситуаций, где ошибка дорога, а действия выполняются редко и по памяти не воспроизводятся.
> Формат каждого: **когда применять → шаги → как проверить → как откатить**.

Незаполненное здесь честнее выдуманного: пиши `<!-- заполнить при первом выполнении -->` и заполняй, когда действие выполняется впервые. Шаг, выполненный один раз и не записанный, во второй раз будет выполнен иначе.

## Выкат

**Когда применять:** {{решили показать изменения пользователям / автоматически при вливании — тогда раздел описывает, что при этом происходит}}.

**Шаги:**

```
{{команда или последовательность действий}}
```

**Как проверить:** {{что открыть или запросить, чтобы убедиться — не «посмотреть, что работает», а конкретная проверка}}

**Сколько занимает:** {{замерить при первом выкате}}

## Откат

**Когда применять:** после выката на проде сломалось.

**Шаги:** {{вернуть изменение по разделу «Откат» в `git-workflow.md`, затем — тот же выкат}}

**Как проверить:** {{та же проверка, что и после выката}}

**Сколько занимает:** {{замерить при первом откате — это ключевая цифра: столько пользователи видят сломанное}}

**Чего откат не делает:** не возвращает данные. Если в выкате была миграция, менявшая схему, возврат кода назад ломает и предыдущую версию — правило разделения миграций в `conventions.md`. В этом случае откат не поможет: нужно выкатывать исправление вперёд.

## Резервная копия и восстановление

**Когда применять:** перед любой операцией, способной потерять данные — удаление таблицы, колонки или индекса, очистка, изменение строк без точного условия, прогон миграций с такими шагами. Правило — в `conventions.md`.

**Снять копию:**

```
{{команда; для PostgreSQL в контейнере — pg_dump -Fc с перечислением задетых таблиц и путём с датой в имени}}
```

**Как проверить:** копия открывается и содержит ожидаемые таблицы — проверять до, а не после разрушающей операции.

**Восстановить:**

```
{{команда восстановления}}
```

**Где лежат копии и сколько живут:** {{путь и срок хранения}}

<!-- Дальше добавляются свои сценарии по мере появления: перезапуск после сбоя, ротация ключей, миграция данных, разбор инцидента. Раздел, который выполняется чаще раза в месяц, стоит автоматизировать, а не описывать. -->
