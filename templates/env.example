# Единственный источник истины по env-переменным проекта (docs/README.md, принцип 7).
# Новая переменная = строка здесь (ИМЯ + комментарий, БЕЗ реального значения) в том же изменении, что и код.
# Реальные значения — в .env (gitignore) и секретах CI/прода.
