## Что нельзя трогать без согласования

- **Секреты и их хранилища** (переменные окружения, ключи, токены) — не коммитить, не переносить, не логировать.
- **Схема данных** — только через миграции; правка модели = миграция в том же изменении.
- **Новые зависимости** — не добавлять без согласования: одной строкой зачем и почему именно эта.
<!-- фрагмент: строку ниже включать только при check=ci — без автоматических проверок менять нечего -->
- {{**Настройки автоматических проверок** — не менять мимоходом внутри рабочей задачи; изменение проверок — отдельная задача.}}

<!-- Пополнять инвариантами проекта по мере появления: расписания фоновых задач, публичные контракты, ключи шифрования. -->
