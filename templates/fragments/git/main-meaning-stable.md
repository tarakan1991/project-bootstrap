## Что означает попадание в `main`

`main` всегда в рабочем состоянии: собирается, проверка проходит. Прода пока нет — ошибка стоит дёшево, но `main` держим рабочим с самого начала: чинить накопившееся потом дороже.

`main` — домашняя база. Между задачами ты стоишь на свежем `main`.
